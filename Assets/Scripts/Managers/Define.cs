using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Define : MonoBehaviour
{
    public enum Sounds { BGM, Effect, MaxCount }
    public enum Scenes { GameScene, TileScene, MaxCount }
}
